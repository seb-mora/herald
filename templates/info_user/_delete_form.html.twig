<form method="post" action="{{ path('app_info_user_delete', {'id': info_user.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ info_user.id) }}">
    <button class="btn">Delete</button>
</form>
